#include <iostream>
#include <vector>
#include <string>
#include <fstream>

using namespace std;

int main() {

	//ternary operator
	//variable = (condition) ? true : false

	int largestNum = (5 > 2) ? 5 : 2;

	//arrays

	return 0;
}